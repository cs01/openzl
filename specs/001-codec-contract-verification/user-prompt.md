# User Prompt

Verbatim input passed to `/speckit-specify`:

```text
can u go hog wild and verify as much as u can with contracts, one by one
```

## Session context

This invocation's arguments were identical to the immediately preceding
conversational message, which had already started the work directly. The user
was asked to disambiguate and chose to write a specification for the work
rather than continue executing it ad hoc.

Prior conversational context that shaped the request, in order:

```text
can u apply the contracts approach we took in ../zstd here? want to find as man
things as we can w/o writing a harness, how hard is it to do this? It's full of
codecs, i.e. prime entry points for intrusion scenarios. we wwanan test there
```

```text
overall can u evaluate how much these contrats pply to htis codebase and if they
will actually improve things?
```

```text
so we actauly formally verified a few are safe? any finds of danger? can u make
the contracts file in simlar format to the zstd one?
```

```text
we found no UB righT? why is there a table on it
```
